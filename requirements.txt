# ============================================================================
# Base requirements — works on Python 3.9+ (system Python on macOS is fine).
# Installs the ONNX INT4 backend ONLY. Use this if you want options 5 and 6.
#
#   pip3 install -r requirements.txt
#
# For the PyTorch backend (options 1–4) you ALSO need to install
# requirements-pytorch.txt, which requires Python >= 3.10.
# ============================================================================

# Core audio + I/O
numpy>=1.21.0
sounddevice>=0.4.0
pynput>=1.7.0
scipy>=1.7.0
huggingface_hub>=0.25.0

# ONNX INT4 backend (options 5, 6) — pure pip wheels, ~30 MB total
onnxruntime>=1.17.0
tokenizers>=0.15.0
